[
  {
    "codeunitId": 60874,
    "CodeunitName": "Test HttpClient",
    "Method": "HttpClient_Get_ValidUrl_ReturnsSuccessResponse",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/1743",
    "Note": "SHOULD be expect-oos: HTTP egress is permanently out of scope (scope.md §3.2) and the runner does throw, with exactly the right message — 'out-of-scope: HttpClient.Get — external-http — see docs/scope.md#external-http'. But the throw is Cecil-injected IL constructing an InvalidOperationException, not a typed RunnerOutOfScopeException, and expect-oos matches on the type. See #1743. Swap this entry to expect-oos (Reason: external-http, file oos-http.json) the moment that lands."
  }
]
