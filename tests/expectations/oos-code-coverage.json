[
  {
    "codeunitId": 60339,
    "CodeunitName": "Test Code Coverage Start Stop",
    "Method": "StartThenQuery_ReportsRecording",
    "Mode": "expect-oos",
    "Reason": "not-yet-implemented",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4468",
    "Doc": "docs/limitations.md#code-coverage-virtual-tables",
    "Note": "Real BC starts code coverage recording and the query form then answers true; that is measured, not assumed -- corpus StefanMaron/BusinessCentral.AL.Language.Tests#387 adds this codeunit and its arms pass on a real service tier. The runner cannot start recording: BC builds a CodeCoverageRecorder over session.NCLMetadata.CodeEnvironment, and ALCodeEnvironment is assigned in exactly ONE place in Ncl.dll (find_usages on NCLMetadata.codeEnvironment returns one FieldWrite, NCLMetadata's own constructor), which needs a NavDatabase and the metadata loaders. MetadataPatches.InjectSkeletonSystemTenant builds that object with RuntimeHelpers.GetUninitializedObject precisely because that constructor cannot run headless, so the field is null BY CONSTRUCTION and no session wiring produces one. Before #3517 the null reached BC's own recorder ctor and surfaced as a bare ArgumentNullException naming the parameter 'codeEnvironment'; CodeCoveragePatches.StartSingleSessionCodeCoverage_Replacement now refuses by name instead. WHY TWO METHODS AND NOT Method '*': the codeunit's other two arms, QueryForm_WithNothingRecording_AnswersFalse and StopWithoutStart_IsTolerated, PASS against the runner -- BC answers both from a null-check over the registered listeners without reading an ALCodeEnvironment, so the runner is faithful there. Read from the BC 27.0.38460.54813 leg of run 35462475735 (job 105949534311), which finished: 2 PASS, 2 FAIL, and those two FAILs are the run's only two failures (3610 pass, 2 fail). A wildcard would claim the two passing arms as failures and drift the other way, which is the mistake al-language-submodule.md records from #3737. WHY expect-oos AND NOT expect-fail-known-gap: the result IS a typed RunnerOutOfScopeException with a reason anchor, which is what expect-oos means. known-gap is not ruled out mechanically -- it accepts any non-pass -- but its authoring rule needs an issue that stays OPEN after merge, and the only candidate is #3517, which the PR adding this entry closes; that is the bookkeeping lie oos-object-metadata.json's Note names for the identical situation. expect-divergence is ruled out mechanically: a test that raises an OOS signal fails that mode with 'declare it expect-oos instead'. WHY THE ANCHOR IS not-yet-implemented AND NOT A docs/scope.md SECTION: the surface is IN scope and unbuilt, so scope.md would assert a permanence that is false; the anchor is also what stops an AL [TryFunction] absorbing the gap into false (ApplicationObjectBasePatches.IsPermanentOutOfScope). WHAT THIS ENTRY CANNOT DISTINGUISH: not-yet-implemented is the shared anchor for every in-scope shape gap, so this entry would also classify a DIFFERENT such refusal raised by this test as green. Bounded rather than absent -- the sibling arms carry no entry, so a refusal that spread across the surface would turn them red and the run would not be quiet. Implementing the surface needs a real ALCodeEnvironment (GetOrAddScopeCounter calls GetOrAssignScopeId) plus the three CodeCoverageDataProvider virtual tables LoadDataIntoVirtualTable fills; docs/limitations.md#code-coverage-virtual-tables states that, and a fix for it removes this file. Written by the agent stma-auto-2 with the #3517 fix."
  },
  {
    "codeunitId": 60339,
    "CodeunitName": "Test Code Coverage Start Stop",
    "Method": "StartOneArgOverload_BehavesAsSingleSession",
    "Mode": "expect-oos",
    "Reason": "not-yet-implemented",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4468",
    "Doc": "docs/limitations.md#code-coverage-virtual-tables",
    "Note": "The one-argument overload, CODECOVERAGELOG(TRUE) -- the spelling BaseApp codeunit 9990 'Code Coverage Mgt.' itself uses. BC forwards it to the two-argument single-session form (ALCodeCoverage.ALCodeCoverageLog(bool) returns ALCodeCoverageLog(newIsActive, multisession: false)), so it reaches the same refusal as the sibling entry above and is the same gap rather than a second one. Listed separately because the entry names one method: the corpus test is a distinct [Test] procedure, and a wildcard would sweep in this codeunit's two PASSING arms. Same run and leg as the sibling entry (BC 27.0.38460.54813, run 35462475735, job 105949534311)."
  }
]
