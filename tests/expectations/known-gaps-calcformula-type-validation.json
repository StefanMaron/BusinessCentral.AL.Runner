[
  {
    "codeunitId": 60442,
    "CodeunitName": "CFM Tests",
    "Method": "Record_CalcFields_Average_FlowFieldTypeDiffersFromSource_Throws",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/2970",
    "Note": "Pulled in by this PR's submodule pin bump, NOT caused by it. The pin advanced to ab6fbefa to consume StefanMaron/BusinessCentral.AL.Language.Tests#174 (the upstream half of #2296); corpus history is linear, so #171's min()/max()/average() CalcFormula tests come with it. Only this one fails: the runner never runs FlowField CalcFormula TYPE validation, so it happily computes an average() whose FlowField type differs from the source field's, where real BC refuses it -- the asserterror therefore finds no error to trap (Runtime.NavMethodScope.AssertError, measured on BC 28.1.49838.53910). The other min()/max()/average() tests from the same corpus PR pass. Tracked as #2970, which stays open after this PR merges; the fix for it removes this entry."
  }
]
