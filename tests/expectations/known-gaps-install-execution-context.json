[
  {
    "codeunitId": 60589,
    "CodeunitName": "Test Install Env Visible",
    "Method": "TestInstallExecCtx_ModuleContextIsInstallPerCompany",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4049",
    "Note": "Expected Install, got Normal. Session.GetExecutionContext() answers Normal inside an install trigger, so the three arms asserting the install context fail. Corpus PR #339 merged at 05:31:40Z and the runner fix (PR #4053) has not; this entry exists so a red main does not wait on that merge, per al-language-submodule.md: a corpus merge that reds main is answered with a fix or a known-gap entry, never by waiting. Methods are named individually because only 3 of this codeunit's 8 arms fail -- a Method wildcard would claim the 5 passing ones and drift the other way (#3737). Remove this file when #4053 lands."
  },
  {
    "codeunitId": 60589,
    "CodeunitName": "Test Install Env Visible",
    "Method": "TestInstallExecCtx_SessionContextIsInstallPerCompany",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4049",
    "Note": "Expected Install, got Normal. Session.GetExecutionContext() answers Normal inside an install trigger, so the three arms asserting the install context fail. Corpus PR #339 merged at 05:31:40Z and the runner fix (PR #4053) has not; this entry exists so a red main does not wait on that merge, per al-language-submodule.md: a corpus merge that reds main is answered with a fix or a known-gap entry, never by waiting. Methods are named individually because only 3 of this codeunit's 8 arms fail -- a Method wildcard would claim the 5 passing ones and drift the other way (#3737). Remove this file when #4053 lands."
  },
  {
    "codeunitId": 60589,
    "CodeunitName": "Test Install Env Visible",
    "Method": "TestInstallExecCtx_SessionContextIsInstallPerDatabase",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4049",
    "Note": "Expected Install, got Normal. Session.GetExecutionContext() answers Normal inside an install trigger, so the three arms asserting the install context fail. Corpus PR #339 merged at 05:31:40Z and the runner fix (PR #4053) has not; this entry exists so a red main does not wait on that merge, per al-language-submodule.md: a corpus merge that reds main is answered with a fix or a known-gap entry, never by waiting. Methods are named individually because only 3 of this codeunit's 8 arms fail -- a Method wildcard would claim the 5 passing ones and drift the other way (#3737). Remove this file when #4053 lands."
  }
]
