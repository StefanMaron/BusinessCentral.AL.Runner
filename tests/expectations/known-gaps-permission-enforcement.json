[
  {
    "codeunitId": 60026,
    "CodeunitName": "Test Perm Mock Enforcement",
    "Method": "MockedSet_WithoutTheGrant_RefusesInsert",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/4578",
    "Note": "An error was expected inside an ASSERTERROR statement. The runner does not enforce permissions (docs/scope.md, Permissions row): a Permissions Mock Set narrows NavTestExecution.EffectivePermissionSets, but BC reads those sets through PermissionManager.GetRolePermissions on NavSession.Permissions, which is null on the runner's skeleton session, so the Insert that real BC refuses goes through. The two sibling arms in the same codeunit (granted insert, cleared assignments) pass. Tracked by #4578, which stays open: faithful enforcement is unimplemented, and this entry records it rather than fixing it."
  }
]
