[
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_NormalKeyField_Throws",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  },
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_NormalTextFieldOnAnotherTable_Throws",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  },
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_FlowFilterField_Throws",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  },
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_FlowFieldWithoutCalcFormula_Throws",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  },
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_BlobAlongsideNormalField_ThrowsForTheNormalField",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  },
  {
    "codeunitId": 60444,
    "CodeunitName": "CalcFields Field Class Tests",
    "Method": "Record_CalcFields_NormalFieldAlongsideValidFlowField_CalculatesNeither",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/3012",
    "Note": "CalcFields must REFUSE a field that is not a FlowField or a BLOB; the runner accepts it silently, so the test's asserterror reports 'An error was expected inside an ASSERTERROR statement'. Tracked in #3012, whose fix is open PR #3079 -- these six are its upstream half and go green when it lands. Green on real BC 27.0 through 28.4. Arrived with the pin bump to corpus 861a5662 in AlRunner#3061's successor and confirmed failing on CI, not merely locally: the BC 27.0 leg of run 34032742416 reports the corpus 2657P/8F naming exactly these six plus the two TPARONH tests. Two tests in this codeunit pass, including Record_CalcFields_BlobField_LoadsTheStoredBytes, so the refusal is missing rather than CalcFields being broken."
  }
]