[
  {
    "codeunitId": 60899,
    "CodeunitName": "Test TxModel AutoRollback",
    "Method": "Test02_PriorAutoRollbackRowDidNotSurvive_ThenWritesItsOwnDefaultRow",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/2503",
    "Note": "Real BC's TransactionModel::AutoRollback per-test attribute rolls a test's own uncommitted write back the moment that test finishes, even under TestIsolation = Codeunit (which on its own would keep the row visible to the next test in the same codeunit -- see the corpus's own TestIsolationRollbackScope). The runner does not implement this override; it applies codeunit-level isolation regardless of the per-test TransactionModel attribute. Test passes on real BC 27.0 through 28.4 -- StefanMaron/BusinessCentral.AL.Language.Tests#114. Tracked as #2503; the fix removes this entry."
  },
  {
    "codeunitId": 60899,
    "CodeunitName": "Test TxModel AutoRollback",
    "Method": "Test03_DefaultModelRowFromPriorTestDidSurvive",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/2503",
    "Note": "Same root cause as Test02 in this file: the runner never rolled back the previous test's AutoRollback row, so this test's own assertion about what SHOULD have survived from the prior (default-model) test observes state left over from the AutoRollback test instead. Test passes on real BC 27.0 through 28.4 -- StefanMaron/BusinessCentral.AL.Language.Tests#114. Tracked as #2503; the fix removes this entry."
  }
]
