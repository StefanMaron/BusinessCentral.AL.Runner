[
  {
    "codeunitId": 60240,
    "CodeunitName": "Test Event Manual Binding",
    "Method": "ManualSubscriber_Test10b_PriorTestsLocalVariableBindingDoesNotFireHere",
    "Mode": "expect-fail-known-gap",
    "Issue": "https://github.com/StefanMaron/BusinessCentral.AL.Runner/issues/2476",
    "Note": "Real BC releases a manual event subscription bound via a LOCAL codeunit variable when the [Test] procedure that declared it returns (NavCodeunit.Dispose(bool) removes the disposed instance's binding from Session.EventBindings). The runner has no equivalent of per-instance disposal for plain, non-SingleInstance codeunit variables, so the binding stays active until the current test CODEUNIT boundary instead (see #2472, which fixes that coarser cross-codeunit leak but does not implement this finer, per-instance one). Test passes on real BC 27.0 through 28.4 -- StefanMaron/BusinessCentral.AL.Language.Tests#110, run 33721610602, all 8 legs green. Tracked as #2476; the fix removes this entry."
  }
]
