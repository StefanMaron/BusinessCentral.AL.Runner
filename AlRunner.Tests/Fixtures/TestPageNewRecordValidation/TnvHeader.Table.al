table 70400 "TNV Header"
{
    DataClassification = SystemMetadata;
    fields { field(1; "No."; Code[20]) { DataClassification = SystemMetadata; } }
    keys { key(PK; "No.") { Clustered = true; } }
}
